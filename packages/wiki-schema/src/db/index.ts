/**
 * index.ts — re-export aggregator for Drizzle schema tables
 *
 * P03 owns this file. Import via "@wiki-team/schema/db" once package exports
 * are wired (see packages/wiki-schema/package.json exports map).
 */

export * from './schema.js';
