# DB schemas (Drizzle) — owned by P03; placeholder dir

This directory is reserved for Drizzle ORM table definitions.

P03 (Storage layer) owns all `*.ts` files here.
P02 (this phase) establishes the directory scaffold only.

See `docs/decisions/011-sync-architecture-optimistic.md` for version column semantics.
