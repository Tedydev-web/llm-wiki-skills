# Changelog (v2.0+ team-mode)

All notable changes to the **monorepo root** (team-mode features) are documented here.
For v1.2 personal-mode history, see [`apps/wiki-skills/CHANGELOG.md`](apps/wiki-skills/CHANGELOG.md).

## [Unreleased]

### Added
- Monorepo conversion: Bun workspaces with `apps/*` + `packages/*`
- `apps/wiki-team/` skeleton (team-mode server)
- `packages/wiki-schema/` skeleton (shared schema definitions)
- `packages/wiki-mcp/` skeleton (MCP integration layer)
- `packages/wiki-shared/` skeleton (shared utilities)
- `tsconfig.base.json` shared TypeScript config
- `skills/` symlink preserving `npx skills add` backward compatibility

---

> v1.2.0 and earlier history archived in [`apps/wiki-skills/CHANGELOG.md`](apps/wiki-skills/CHANGELOG.md)
